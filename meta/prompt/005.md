## 連接API:
- 目前ChatCommand是寫死有哪些功能的，目前我想要接收: https:chienan0617.github.io/layout/dev.cas.learnio/commands/commands_list.json裡面的東西，裡面的結構長:

```json
[
  ...
  {
    "name": "總結對話",
    "desc": "讓AI總結目前的對話內容",
    "icon": "summarize_outlined",
    "output": "請總結目前對話", //-> 該send甚麼訊息
    "enable": true,
  },
  ...
]
```

我想要fetch然後icon搭配core lib的iconData 來from string to IconData

## 修改代碼邏輯
- 我的command有了fetch function但沒有實作阿。
- 順便幫我整個APP UI只要有async的等待回傳都設計skeleton loading