@contextScopeItemMention 參考DESIN.md
並幫我重新設計所有介面:
你現在要修改我已經生成好的 Flutter 代碼，目標是把整個 App 的介面統一成「現代、簡潔、乾淨、同一套視覺語言」。
不要重寫成另一個風格，不要增加花俏裝飾，不要讓每個頁面長得不一樣。

最高優先級仍然是：

- metadata/RULES.md
- metadata/LAWS.md

修改目標：

1. 保持整體 UI 一致性
2. 統一設計系統
3. 讓所有頁面看起來像同一個產品
4. 保留現代 AI App 風格，接近 ChatGPT / Gemini 的乾淨感
5. 只做必要修改，不破壞現有架構與邏輯分層

請你先做這些事：

- 掃描現有專案的所有頁面、元件、顏色、字體、間距、圓角、陰影、按鈕、輸入框、卡片、側邊欄、頂部列、底部操作列
- 找出不一致的地方
- 建立或統一一套 design system
- 讓所有頁面共用相同的 token 與元件

統一規範：

- 色彩：只使用少量主色、輔色、背景色、文字色、分隔色
- 字體：統一字重、標題層級、字級尺度
- 間距：統一 padding / margin / gap 尺度，不要每頁亂跳
- 圓角：統一卡片、按鈕、輸入框的 radius
- 陰影：盡量輕，保持乾淨，不要重陰影
- 圖示：統一線條粗細與風格
- 卡片：統一高度、內距、邊框與 hover / pressed 狀態
- 輸入框：統一樣式，包含模型選擇、語音、附件、送出按鈕的位置與比例
- 側邊欄與聊天頁：保持同一套導航與資訊密度
- 空白區域：保留呼吸感，但不要過度留白導致內容稀薄

修改原則：

- 不要在不同頁面使用不同風格的 component
- 不要在同一個 App 裡混用太多陰影、漸層、邊框
- 不要把功能性元件做得像不同產品
- 不要為了漂亮而犧牲可讀性
- 優先透過共用元件與 theme 統一，而不是逐頁硬改

實作方式：

- 先整理 theme / design tokens / shared widgets
- 再逐頁替換成統一元件
- 保持 UI 與邏輯分離
- 若發現舊程式有重複寫法，先抽成共用元件
- 若某頁偏離整體風格，優先修正它來對齊主設計

我希望最後得到的是：

- 一套一致的視覺系統
- 所有頁面像同一個 AI App
- 簡潔、現代、專業、可維護
- 不改壞現有功能
- 不新增多餘花樣

請直接開始修改代碼。
先輸出你要統一的設計規則，再開始改檔案。
修改完後，列出你改了哪些檔案，以及哪些地方已經統一。
