## 目前有以下幾點問題，請一併解決
- 我只要點擊對話紀錄，我只能看到我問的的第一個問題，他回答得和之後所對話的都沒辦法知道，是沒紀錄還是顯示不出來
- 幫我重新設計chat bar底下的typing area, 讓他附加檔案和選擇模型和指令的風格一致，跟主流AI聊天機器人一樣，
像gemini, chatgpt, grok, claude一樣
- 我送出訊息後他會有一個BUG，就是他會同時冒出正在輸入或思考中的氣泡，但同時又多出一個沒有對話assistant的輸出框，等到他有訊息串流書來的時候，思考中的氣泡消失，開始有assistant的對話。幫我解決他正在思考但有assistant的空對話的問題
- 重新設計sidebar讓他更像現代APP
- 在專案中多加入毛玻璃等透明效果，例如輸入框或其他合適的場景
- 重新設計主畫面
- 整體使用更簡潔的設計，讓畫面更舒服，不要那麼複雜
- 幫我把有logo的地方都套換成 @assets/icon/icon.webp我要用我自己的logo
- 送出後取消回應的按鈕(原本送出按鈕的位置)，icon和bg都是白色的對比度不高，請重新設計
- 把學習庫和聊天session從sidebar拿掉
- 他在輸出時可以為logo增加動畫嗎，不要用到圖片，我要用到其他方法讓它動起來
- 最近的對話中，我不想要prefix顯示chat icon，底下要不要顯示時間加到settings裡面讓使用者自己設定

## 需要改進:
- setting page 從 side bar進入有問題:
__type 'Null' is not a subtype of type 'bool'__
- 我只告訴你要刪除學習庫和聊天在sidebar我其他的像是 儲存庫那些不要刪除
- 我開啟我之前的對話他會顯示錯誤:
```txt
Exception has occurred. X

FlutterError (_MessageBubbleState is a SingleTickerProviderStateMixin but multiple tickers were created.

A SingleTickerProviderStateMixin can only be used as a TickerProvider once.

If a State is used for multiple AnimationController objects, or if it is passed to other objects and those objects might use it more than one time in total, then instead of mixing in a

SingleTickerProviderStateMixin, use a regular TickerProviderStateMixin.)
```
- 我送出訊息後他會顯示錯誤:
```txt
Exception has occurred. X

FlutterError (_MessageBubbleState is a SingleTickerProviderStateMixin but multiple tickers were created.

A SingleTickerProviderStateMixin can only be used as a TickerProvider once.

If a State is used for multiple AnimationController objects, or if it is passed to other objects and those objects might use it more than one time in total, then instead of mixing in a

SingleTickerProviderStateMixin, use a regular TickerProviderStateMixin.)
```
- 我的毛玻璃效果也沒套用在整個APP阿
- 重新設計整個typing area包括選擇檔案和選擇模型和選擇指令，另訂方案不要從現有的設計下卻修改，
- 整體畫面和UI要更清爽、簡潔、現代
- 我要在整個APP套用更豐富的動畫

## 需要修正:
- 我要的是typing area 和其他合適合的地方用毛玻璃笑果
- 背景是甚麼大便顏色，背景用深色就好
- 幫我重新設計ChatInputBar的所有東西， **DO NOT REDESIGN FROM ORIGINAL DESIGN**