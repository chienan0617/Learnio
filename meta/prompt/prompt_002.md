你現在要直接實作整個 Flutter App 專案，不是只做規劃。

先讀取並嚴格遵守：
- metadata/RULES.md
- metadata/LAWS.md

它們是最高優先級條件。你必須以這兩份文件為準來產出完整程式碼。

你的任務是：
1. 產出整個 App 的 Flutter 程式碼
2. 頁面與邏輯必須分離，完全依照 RULES.md 的檔案結構與分層方式
3. 不能把 UI、狀態、商業邏輯、資料層混在同一個檔案
4. 必須建立完整可執行的專案，而不是只有片段或 demo
5. 必須把我要求的所有頁面、元件、互動與基本流程一起做出來
6. 如果規則沒有明確寫死某些細節，你要採用最合理、最符合現代 AI App 的做法，並保持一致性
7. 不要只回覆說明文字；要直接輸出可以落地的檔案內容
8. 除非真的無法繼續，否則不要反問我；你要先根據條件自行完成

這個 App 的方向是「學習導向的 AI 聊天 App」，風格要像 Gemini / ChatGPT：
- 現代、簡潔、乾淨
- 聊天主畫面
- 初始首頁
- 側邊欄
- 對話紀錄
- 新對話
- 搜尋
- 專案
- 收藏
- 學習庫
- 設定
- 聊天室輸入框
- 模型選擇
- 語音按鈕
- 附加檔案按鈕
- 送出按鈕

實作要求：
- Flutter
- 依 RULES.md 的結構建立檔案
- page / widget / controller / service / model / repository 分層清楚
- 使用可重用元件
- 狀態管理要一致
- 路由要完整
- 側邊欄與聊天室互動要正常
- 列表、搜尋、專案、收藏、歷史紀錄等資料流程要能運作
- 預設就要有完整的 mock data 或 local data flow，讓畫面與邏輯都能跑起來
- 程式碼要能編譯，import 要正確，命名要一致
- 不要省略核心檔案

執行方式：
先依 RULES.md 產生完整檔案樹，
接著直接開始寫檔案內容，
最後自行檢查一次整體是否符合 RULES.md / LAWS.md。

輸出格式：
- 先列出你將建立的檔案樹
- 再逐檔輸出程式碼
- 最後列出驗證結果與剩餘待補項目

現在開始，直接產出整個專案。